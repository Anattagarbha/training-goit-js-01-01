"use strict";

// number - целые числа и числа с плавающей точкой.

const age = 20;
const points = 15.8;

// string - строки, последовательность из 0 и более символов.

const username = "Mango";
const description = "JavaScript для начинающих";

// boolean - логический тип данных, флаги состояния true и false.
// Имена переменных, содержащих логический тип, задают вопрос:

const isLoggedIn = true;
const canMerge = false;

// null - специальное значение, указывающее на ничто, пустоту.

const selectedProduct = null;

// undefined - специальное значение, указывающее на объявленную,
// но неинициализированную переменную.

let userAge;

console.log(
  age,
  points,
  username,
  description,
  isLoggedIn,
  canMerge,
  selectedProduct,
  userAge
);
